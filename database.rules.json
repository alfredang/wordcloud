{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        "submissions": {
          "$submissionId": {
            ".validate": "newData.hasChildren(['word', 'normalizedWord', 'timestamp', 'roomId'])"
          }
        }
      }
    }
  }
}
